<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leolai.earthquake" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".EarthQuake"
            android:label="@string/app_name" >
            <!--android:name=".EarthQuakeOld"-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".QuakePreferenceActivity"
            android:label="@string/title_activity_preference" >
        </activity>
        <activity
            android:name=".preference.UserPreferences"
            android:label="@string/title_activity_preference" >
        </activity>
        <activity android:name=".preference.FragementPreference"/>
    </application>

</manifest>
